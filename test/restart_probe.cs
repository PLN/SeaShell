//sea_restart
// Probe: does the Invoker see Restart=true?
// This script doesn't use any new Sea properties — it should compile
// on any version of SeaShell.Script.dll.
// The Invoker log "restart mode active" tells us if Restart flowed through.
Console.WriteLine("restart_probe: running");
return 0;
